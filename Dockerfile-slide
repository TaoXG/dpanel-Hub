# 基础镜像：选择Python 3.9-slim（轻量且兼容大部分依赖）
FROM python:3.9-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖（OpenCV 需要的底层库）
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgl1-mesa-glx \
    libglib2.0-0 \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件（先复制requirements.txt，利用Docker缓存）
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -i https://pypi.tuna.tsinghua.edu.cn/simple \
    opencv-python \
    numpy \
    bottle

# 复制脚本文件到工作目录
COPY slide.py .

# 暴露端口（与脚本中port=3001对应）
EXPOSE 3001

# 设置容器启动命令（确保服务持续运行，即使崩溃也重启）
CMD ["python"， "-u", "slide.py"]
